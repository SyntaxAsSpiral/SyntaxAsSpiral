# LLM Configuration for SyntaxAsSpiral Pulse Generator
# This file overrides global .env settings for this repo
# Note API keys are stored in secrets.env and are not tracked by git

# Primary backend (try first)
LLM_PRIMARY_PROVIDER=openrouter
LLM_PRIMARY_BASE_URL=https://openrouter.ai/api/v1
LLM_PRIMARY_MODEL=deepseek/deepseek-v3.2

 # Fallback backend (try if primary unreachable)
 LLM_FALLBACK_PROVIDER=lmstudio
LLM_FALLBACK_BASE_URL=http://localhost:1234/v1
LLM_FALLBACK_MODEL=gpt-oss-20b-heretic

# Fast-fail if both unreachable
